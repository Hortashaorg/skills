{
  "name": "skills",
  "private": "true",
  "version": "1.0.0",
  "scripts": {
    "local": "infisical run --env=local --path=/ -- pnpm",
    "all": "pnpm -r --paralell",
    "frontend": "pnpm --filter '@service/frontend'",
    "backend": "pnpm --filter '@service/backend'",
    "check": "biome check",
    "format": "biome format --write",
    "database": "pnpm --filter '@package/database'"
  },
  "author": "Martin Eide",
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "node-gyp": "^12.1.0",
    "typescript": "^5.9.3"
  },
  "trustedDependencies": [
    "@rocicorp/zero-sqlite3"
  ],
  "pnpm": {
    "onlyBuiltDependencies": [
      "@rocicorp/zero-sqlite3"
    ]
  }
}
